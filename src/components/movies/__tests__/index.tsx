import Movies from "../index";

describe("Handling rendering list of movies", () => { 
    it("should compile at runtime", () => { 
        expect(Movies).toBeDefined();
    });

    it("should show a message when there are no movies to show", () => { 
        const expected = "No movies to show";
        //todo
    });

    it("Should show the correct number of movies", () => {

    });
})